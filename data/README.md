# This folder stores auto-generated JSON backups of the bot's database tables.
# These files are committed to GitHub so data persists across Render restarts.
# Do NOT manually edit these files — they are auto-managed by git_sync.py.
